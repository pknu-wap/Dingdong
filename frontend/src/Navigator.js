import React from 'react';
import "./App.css";

const Navigator = () => {
    return (
        <navigator>
            <nav>
                전체 물품
            </nav>
            <nav>
                마이 페이지
            </nav>
        </navigator>
    );
};

export default Navigator;
